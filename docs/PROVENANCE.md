# Provenance

The production build has no dependency on `opensource-references/`.

| Artifact | Source and revision | License | Treatment |
|---|---|---|---|
| PangCrypt algorithm, oracle tables, vectors | `pangbox/pangcrypt` `2bf7a1d36591`; local paths `common.go`, `client.go`, `server.go`, `*_test.go` | ISC | Adapted with notice; oracle SHA-256 locked by test |
| Login packet layouts and opcode evidence | `pangbox/packetdoc` `d61f583a3e67`, `src/packets/loginservice/{client,server}` | ISC | Layout evidence; unknown bytes stay explicit |
| U.S. 852 Login hello | `pangbox/server` `91d8a5a4f3be`, `login/conn.go`, `login/msgserver.go` | ISC | 14-byte key-parametric synthetic fixture |
| LZO implementation | crates.io `lzokay` 2.x | MIT | Dependency; compatibility gates documented |
| M3/M5/M6 catalog fixtures | Locally generated by `pangya-rs`, 2026-08-05; `crates/pangya-data/tests/fixtures/synthetic-catalog` | MIT OR Apache-2.0 | Original three eight-byte M3 records plus a 13-byte `Course.bin` containing only the synthetic header, type ID, and local one-hole par; reused by both synthetic one-hole modes; per-file SHA-256 in manifest; no client/IFF source bytes |
| M3 Game hello fixture | Locally generated by `pangya-rs`, 2026-08-05; `crates/pangya-protocol/src/game.rs` | MIT OR Apache-2.0 | Four synthetic bytes; only key-final-byte behavior is externally informed |
| M4 room packet fixtures | Locally generated by `pangya-rs`, 2026-08-05; `crates/pangya-protocol/tests/fixtures/m4-*-synthetic` | MIT OR Apache-2.0 | Four binary/YAML pairs for create, list, state, and chat; generated from the local `0x7f00` model with no upstream/client source bytes |
| M5 solo packet fixtures | Locally generated by `pangya-rs`, 2026-08-05; `crates/pangya-protocol/tests/fixtures/m5-*-synthetic` | MIT OR Apache-2.0 | Five binary/YAML pairs for start, match-started, shot action, shot result, and hole result; generated from the explicit local-only `0x7f20`/`0x7fa0` model with SHA-256 metadata and no upstream/client source bytes |
| M6 stroke packet fixtures | Locally generated by `pangya-rs`, 2026-08-05; `crates/pangya-protocol/tests/fixtures/m6-*-synthetic` | MIT OR Apache-2.0 | Fourteen binary/YAML pairs covering every generated C -> S `0x7f30..=0x7f34` and S -> C `0x7fb0..=0x7fb8` layout; per-fixture SHA-256 is recorded in YAML and M6 evidence; no upstream/client source bytes |
| M7 economy packet fixtures | Locally generated by `pangya-rs`, 2026-08-07; `crates/pangya-protocol/tests/fixtures/m7-*-synthetic` | MIT OR Apache-2.0 | Eleven binary/YAML pairs covering every generated C -> S `0x7f40..=0x7f44` and S -> C `0x7fc0..=0x7fc5` layout; per-fixture SHA-256 is recorded in YAML and M7 evidence; no upstream/client source bytes |
| M7 catalog fixture | Locally generated by `pangya-rs`, 2026-08-07; `crates/pangya-data/tests/fixtures/synthetic-catalog-v2` | MIT OR Apache-2.0 | Generated shop/consumable/durability catalog; per-file SHA-256 recorded in its `manifest.toml`; no client IFF bytes |
| Client patch web contract: XTEA, file CRC32, updatelist/litexml layout | `pangbox/pangfiles`, local paths `crypto/pyxtea/{xtea,keys}.go`, `hash/pycrc32/crc.go`, `updatelist/xml.go`, `encoding/litexml/{emit,encode}.go`; endpoint set from `pangbox/server` `web/{handler,data}.go` | ISC | Algorithms and document layout adapted with notice in `crates/pangya-updater`; the Rust implementation, bounds, and error model are original. Byte equality with the reference encoder is pinned by a golden fixture and was additionally confirmed against the real 84-file U.S. PAK directory |
| U.S. client artifact | Acquired 2026-08-07 from a community preservation archive; retained only under gitignored `local-data/` | Proprietary; not redistributed | Identity, patch level, and what static inspection could and could not establish are recorded in `evidence/US_CLIENT_ACQUISITION_2026-08-07.md`; no client bytes, PAK contents, or IFF records are committed |
| ISSUE11 My Room layouts | `pangbox/packetdoc` `d61f583a3e67`, `gameservice/client/{00b5,00b7,00b9}.ksy`, `gameservice/server/{012b,012d,012e}.ksy` | ISC | Reference-derived field widths and opaque bytes only; no live-client acceptance claim |
| ISSUE11 mascot message flow | `Acrisio-Filho/SuperSS-Dev` `485ba29b4831`, `Server Lib/Game Server/GAME/channel.cpp`, `requestChangeMascotMessage` | MIT | Behavior evidence: authenticated owner update persists then emits `0x00e2`; the response is not inferred as an unsolicited visitor-room packet |

Fixtures contain no client assets or attested real account data. Most values are
synthetic. The exact upstream PangCrypt/PacketDoc `john` vector is a published
dummy test vector whose digest is the well-known unsalted MD5 of the literal
secret `test`; it is neither irreversible nor evidence of a real account. Its
identity is not independently attested, and the bytes are preserved exactly
because the protocol specification requires all three upstream vectors.
GPL/AGPL/unlicensed references were not copied and remain behavior-only. M3 IFF
headers, record sizes, bootstrap layouts, and packet ordering are synthetic and
must not be described as extracted from or accepted by a U.S. 852 client. The M4
`0x7f00`/`0x7f80` opcode families, room layouts, and ordering are likewise wholly
generated local contracts and are not sourced from or accepted by U.S. 852.
The same applies to M5 `0x7f20`/`0x7fa0`, M6 `0x7f30`/`0x7fb0`, M7
`0x7f40`/`0x7fc0`, generated conditions/standings/rewards/record rules, generated
prices/stack limits/durability and repair rates, and all local packet ordering.
The acquired U.S. client is referenced for characterization only; no byte of it,
of its PAK archives, or of its IFF records is committed, and the asset guard plus
`.gitignore` enforce that mechanically.

The `updatelist` cipher and checksum are reproduced exactly rather than normalised. The checksum is not CRC-32/ISO-HDLC: upstream generates a reflected table from the normal-form constant `0x04C11DB7`, and the client expects that result, so substituting `0xEDB88320` would break it. The XTEA variant subtracts its delta while encrypting. Both are documented at their definitions in `crates/pangya-updater`.
