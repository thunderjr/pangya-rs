# Copy to an ignored .env and replace this obvious non-secret placeholder.
DATABASE_URL=postgres://pangya:REPLACE_WITH_LOCAL_PASSWORD@127.0.0.1:5432/pangya
# Example account-create secret source name; never pass the 32-hex value as a CLI argument.
PANGYA_ACCOUNT_SECRET=REPLACE_WITH_SYNTHETIC_32_HEX_VALUE
